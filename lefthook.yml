pre-commit:
  parallel: true
  commands:
    swift-format:
      glob: "apple/**/*.swift"
      run: xcrun swift-format format --in-place {staged_files}
      stage_fixed: true
      fail_text: Swift のフォーマットエラーがあります
